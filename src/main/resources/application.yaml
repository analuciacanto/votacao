spring:
  application:
    name: votacao
  datasource:
    url: jdbc:h2:file:./data/votacao-db   # arquivo persistente
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update                   # mantém dados entre restarts
    show-sql: true
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080